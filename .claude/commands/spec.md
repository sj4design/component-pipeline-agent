---
allowed-tools: Read, Write, Edit, Glob, Grep, Bash, Agent, TodoWrite
description: "Generate full component spec (anatomy + matrix + optimize) in one pass"
---

# /spec $ARGUMENTS

Read `spec-agent/SKILL.md` first, then execute.

Output: `outputs/$ARGUMENTS-config.json` (always) + `outputs/$ARGUMENTS-spec.md` (only with --human flag)

The component name is: $ARGUMENTS
