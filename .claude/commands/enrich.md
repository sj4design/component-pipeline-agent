---
allowed-tools: Read, Write, Edit, Glob, Grep, Bash, Agent, TodoWrite
description: "Add interaction spec + tokens to a component (combined agent)"
---

# /enrich $ARGUMENTS

Read `enrich-agent/SKILL.md` first, then execute.

Input: `outputs/$ARGUMENTS-config.json` (PRIMARY — do NOT read spec.md)
Output: `outputs/$ARGUMENTS-enriched.md` + updated `outputs/$ARGUMENTS-config.json`

The component name is: $ARGUMENTS
