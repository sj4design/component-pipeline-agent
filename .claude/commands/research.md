---
allowed-tools: Read, Write, Edit, Glob, Grep, Bash, Agent, TodoWrite
description: "Research a UI component across major design systems"
---

# /research $ARGUMENTS

Read `component-research-agent/SKILL.md` first, then execute.

Output: `research/components/$ARGUMENTS.md`

Flags: --max (no scope, ALL patterns), --fresh (ignore cache)

The component name is: $ARGUMENTS
